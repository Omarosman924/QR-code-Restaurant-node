<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
</head>


<body>
   <% tables.forEach((table)=>{ %>
    <a href="/menu/<%= table._id %>" > Tab_id</a>
    <br>

    <% }); %>

</body>
